# 网络调试工具

一个基于React和Node.js的网络调试工具，提供端口扫描、DNS解析、Ping和MTR等功能。

## 功能特性

- 🏠 **首页展示**：自动检测并显示当前设备的IPv4和IPv6地址
- 🔍 **端口扫描**：支持IPv4和IPv6，常用端口和自定义端口扫描
- 🌐 **DNS解析**：解析域名的各种DNS记录（A、AAAA、MX、NS、TXT等）
- 📡 **Ping测试**：网络连通性和延迟测试
- 🛣️ **MTR诊断**：网络路径追踪和诊断

## 技术栈

### 前端
- React 18
- React Router
- Material-UI (MUI)
- Axios

### 后端
- Node.js
- Express
- 原生Node.js模块（net、dns、child_process）

## 安装和运行

### 前置要求
- Node.js >= 14
- npm 或 yarn
- mtr工具（仅MTR功能需要，可选）

### 安装依赖

```bash
npm run install-all
```

### 运行项目

开发模式（同时启动前端和后端）：

```bash
npm run dev
```

或者分别启动：

```bash
# 启动后端（端口3001）
npm run server

# 启动前端（端口3000）
npm run client
```

### 访问应用

打开浏览器访问：http://localhost:3000

## 项目结构

```
NetworkDebug/
├── backend/           # 后端API服务
│   ├── server.js     # Express服务器
│   └── package.json
├── frontend/         # React前端应用
│   ├── src/
│   │   ├── pages/    # 页面组件
│   │   ├── components/ # 公共组件
│   │   └── App.js
│   └── package.json
└── package.json      # 根package.json
```

## API接口

### GET /api/ip-addresses
获取本机IP地址

### POST /api/port-scan
端口扫描
```json
{
  "host": "192.168.1.1",
  "ports": [80, 443, 8080],
  "timeout": 3000
}
```

### POST /api/dns-resolve
DNS解析
```json
{
  "hostname": "example.com"
}
```

### POST /api/ping
Ping测试
```json
{
  "host": "8.8.8.8",
  "count": 4
}
```

### POST /api/mtr
MTR诊断
```json
{
  "host": "8.8.8.8",
  "count": 10
}
```

## 注意事项

1. **MTR功能**：需要系统安装mtr工具
   - macOS: `brew install mtr`
   - Linux: `apt-get install mtr-tiny` 或 `yum install mtr`

2. **端口扫描**：扫描大量端口可能需要较长时间，建议合理设置超时时间

3. **权限要求**：某些网络操作可能需要管理员权限

## 许可证

MIT

